export class Post{
  constructor(
  title: String,
  subtitle: String,
  imageUrl: String,
  category: String,
  content: String){
      title = title,
      subtitle = subtitle,
      imageUrl = imageUrl,
      category = category,
      content = content
  }
}